import { BookmarkDirective } from './bookmark.directive';

describe('BookmarkDirective', () => {
  it('should create an instance', () => {
    const directive = new BookmarkDirective();
    expect(directive).toBeTruthy();
  });
});
